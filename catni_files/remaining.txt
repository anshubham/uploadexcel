import sets

with open("/home/shubham/Desktop/qqq.txt") as f:
	list_initial_emails = f.readlines()
list_initial_emails = map(lambda s: s.strip(), list_initial_emails)

with open("/home/shubham/Desktop/email_status.txt") as f:
	list_processed_emails = f.readlines()

list_splitted_emails = []
for ele in list_processed_emails:
	ele = ele.split("\t")
	list_splitted_emails.append(ele)

processed_email_ids = []
for ele in list_splitted_emails:
	try:
		email = ele[1]
	except IndexError:
		email = 'null'
	processed_email_ids.append(email)

remaining_emails_to_process = []

remaining_emails_to_process=list(set(list_initial_emails)-set(processed_email_ids))
